environments:
  - name: aec
    endpoints:
      production:
      sandbox:
  - name: dev
    endpoints:
      production:
      sandbox:

deploy:
  import:
    update: true
    preserveProvider: true
